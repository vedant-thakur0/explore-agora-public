[pytest]
# Put the repo root on sys.path so tests import the top-level `pipeline` package
# (run from the repo root: `python3 -m pytest`).
pythonpath = .
testpaths = pipeline/tests
python_files = test_*.py
